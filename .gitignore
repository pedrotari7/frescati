node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# builds
backend/lib/
dist/
*.tsbuildinfo

# env
.env
.env.local
.env*.local
*-service-account.json

# ...except the functions' deploy-time parameters, which are not secrets and
# which `firebase deploy` needs in the repo: a non-interactive deploy with a
# declared param missing from a dotenv file fails rather than taking a default.
!backend/.env

# firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
pubsub-debug.log

# vercel
.vercel

# test
coverage/

# editors / os
.DS_Store
*.swp

# seeded local fixtures (pnpm seed)
frontend/public/dev-users.json
frontend/public/dev-avatars/
