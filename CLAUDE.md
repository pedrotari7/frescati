# CLAUDE.md

Everything is in `AGENTS.md`, which this file imports below. One copy rather than two, so the halves cannot drift and a tool that reads only one of these files still gets all of it.

<!-- fallow:agent-install v1 claude-import:start -->

@AGENTS.md

<!-- fallow:agent-install v1 claude-import:end -->
